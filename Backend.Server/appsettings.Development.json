{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },

  // Uncomment to use MySQL instead of SQL Server
  //"PayrollServerConfiguration": {
  //  "DbProvider": "MySql"
  //},

  // MySQL connection string (Port 3307 as used in local PoC)
  // NOTE: for local testing use root -- create a dedicated 'payroll' user for production
  //"ConnectionStrings": {
  //  "PayrollDatabaseConnection": "Server=localhost;Port=3307;Database=PayrollEngine;User=root;Password=PayrollPoc2026;CharSet=utf8mb4;"
  //}
}
